# ======================
# JinYu 前台配置模板
# 复制本文件为 .env 并填写实际值
# ======================

# 前台监听端口（默认 3011）
PORT=3011

# 后台服务地址（同机器时用 127.0.0.1，不同机器时填内网/公网 IP）
ADMIN_HOST=127.0.0.1

# 后台服务端口（默认 3020）
ADMIN_PORT=3020

# 后台通信协议：http 或 https（默认 http；若后台用 HTTPS 则改为 https）
ADMIN_PROTOCOL=http
